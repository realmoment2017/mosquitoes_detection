import numpy as np
import cv2

IMG_upper_left_x = 0 
IMG_upper_left_y = 0
WIDTH = 1600
HEIGHT = 1200

tune_mode = False

#cap = cv2.VideoCapture(0)
#cap.set(3,1600)
#cap.set(4,1200)
#cap.set(cv2.CAP_PROP_BRIGHTNESS, 40)
#cap.set(cv2.CAP_PROP_CONTRAST, 100)
